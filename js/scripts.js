$('.checkin input[type=data]').each(function(){
    var input = $(this); // This is the jquery object of the input, do what you will
   });

$('.checkin input:button').click(function(){
    // $('.choose select').slideDown('slow')
    // $('input').slideDown('slow')

    $('send').text()
})